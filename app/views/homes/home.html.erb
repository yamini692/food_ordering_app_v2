<% if user_signed_in? %>
  Logged in as <%= current_user.email %> |
  <%= link_to "Logout", destroy_user_session_path, method: :delete, data: { confirm: "Are you sure?", turbo: false } %>
<% else %>
  <%= link_to "Login", new_user_session_path %>
<% end %>
